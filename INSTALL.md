1. Descarga el zip.
2. Instala Python 3.10 o superior.
3. Instala `adb` y `scrcpy` (o coloca `adb` en `config/tools/platform-tools`).
4. Activa la **Depuración USB** en el dispositivo Android.
5. Ejecuta:

```
python start.py
```
